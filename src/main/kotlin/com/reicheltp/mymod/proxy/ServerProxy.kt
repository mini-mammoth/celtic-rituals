package com.reicheltp.mymod.proxy

/**
 * Register stuff we need on server side only. Usually used to register dimensions.
 *
 * @See CommonProxy for registrations on both sides.
 * @see ClientProxy for client-side only registrations.
 */
class ServerProxy: CommonProxy() {
}